# Project type catalog

Built-in Project Type definitions are authored in
`studio-api/app/templates_presets/catalog/project_types.py` and loaded at runtime.

JSON packages (`adept.project_type.v1`) may be imported via the Templates/Presets API
for custom types. Marketplace/community sharing is out of M3.1a scope.
