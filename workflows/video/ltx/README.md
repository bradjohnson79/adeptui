# LTX video workflow mirrors

Runtime authority remains `config/video-workflows/certified-registry.json` and `studio-api/app/workflows/ltx_builder.py`.
This folder exists for provider-owned organization alongside `hunyuan15/` and `hunyuan13b/`.
