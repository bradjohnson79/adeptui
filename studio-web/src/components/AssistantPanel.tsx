/**
 * @deprecated Use `CoDirectorHost` / `useOpenCoDirector` from `./CoDirector`.
 * Kept as a compatibility shim so older imports do not crash during cutover.
 */
export function AssistantPanel(_props: Record<string, unknown>) {
  return null;
}
