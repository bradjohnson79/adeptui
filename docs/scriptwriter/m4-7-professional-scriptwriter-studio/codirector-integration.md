# Co-Director Integration

Wire `activeDocumentId`, scene/character/location/revision context. Mutating tools use proposal → review → `apply_codirector_proposal` transaction. Never silent script mutation.
