# Production Integration

Bible entity detection → proposals only. Scene sync statuses. Timeline prep → propose/approve → `apply_timeline_prep_metadata`. PI visualization prompts stay outside screenplay prose.
