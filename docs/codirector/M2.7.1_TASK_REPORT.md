# Task Report -- Co-Director M2.7.1 Closure (Demock + Production Context)

**Date:** 2026-07-25  
**Workspace:** `C:\AdeptFilmWorks\AIVideoStudio`  
**Branch:** `phase2/codirector-m2-7-production-executive`  
**Tip SHA:** `35ca17b`   
**Draft PR:** https://github.com/bradjohnson79/adeptui/pull/2 -> `develop`  
**Task verdict:** **NOT Accepted** — real-provider closed-loop must pass live ComfyUI + vision before Accepted

---

## Summary

M2.7.1 closure removes silent mock ImageGen from the Adept UI runtime path, adds immutable Production Context (M011 / `M011`), and retargets acceptance/Playwright at real Comfy + local vision. Feature flag `STUDIO_FEATURE_PRODUCTION_EXECUTIVE_V1` remains **default OFF**.

**M2.7 is accepted only together with M2.7.1 real-provider criteria** (see `docs/codirector/m2.7.1-acceptance.md`).

---

## What changed (closure)

### Production Context (M011)

- `production_context.py` — frozen dataclass, `to_dict`/`from_dict` omit None, create once, append-only extensions + audit
- Migration `m011_production_context.py` revision `M011` after M010 (M009 unused)
- ORM: `ProductionContextRow`, `ProductionContextExtension`, `ProductionJob.production_context_id`
- `create_closed_loop` creates context once, attaches to every job + payload snapshot

### Demock

- `imagegen_adapter.py` — no `complete_mock_imagegen_job` / placeholder PNG; poll real jobs only
- `capability.py` — no ADEPT_MOCK_IMAGEGEN/STUDIO_E2E `comfyui.health` fake
- Handlers/service/api/contracts/worker — provider defaults `local`/`comfy`; reject validate `mock`; no fixtureProfile pass default
- `scripts/e2e-start.mjs` — removed `ADEPT_MOCK_IMAGEGEN || "1"`
- UI — `provider: "local"`

### Tests / Playwright

- Pytest: no autouse mock ImageGen; closed-loop/image tests skip without Comfy; added context/M011 unit tests
- Playwright: infrastructure probe; `provider: "local"`; real artifact assertions

---

## Verification

| Suite | Expected |
|-------|----------|
| Pytest unit/schema/migration | Pass without Comfy |
| Pytest closed-loop/image | Skip or pass only with live Comfy |
| Playwright closed-loop | Skip with infrastructure reason or pass with live Comfy |
| Accepted | **No** until real-provider path passes |

---


---

## Environment honesty (tip `35ca17b`)

**Status: NOT Accepted.** In this environment Comfy health may report OK while `storyboard.generate` / ImageGen packs are not catalogued — Playwright must **skip or fail honestly**, never mock-pass.

Production Context **M011** + demock have landed. Closed-loop proof requires BOTH `comfyui.health` and `storyboard.generate` (and `codirector.vision.validate` when exposed).

## Key paths

| Area | Path |
|------|------|
| Production Context | `studio-api/app/codirector/executive/production_context.py` |
| M011 | `studio-api/app/migrations/m011_production_context.py` |
| ImageGen adapter | `studio-api/app/codirector/executive/imagegen_adapter.py` |
| Handlers / service | `handlers.py`, `service.py` |
| Pytest | `studio-api/tests/test_production_executive.py` |
| Playwright | `tests/e2e/codirector/production-executive-m27.spec.ts` |

### Docs

| File | Purpose |
|------|---------|
| `m2.7.1-production-context.md` | Context contract |
| `m2.7.1-integration.md` | Real wiring (democked) |
| `m2.7.1-worker-lifecycle.md` | Worker + honest capabilities |
| `m2.7.1-acceptance.md` | Real-provider checklist |
| `m2.7.1-completion.md` | Status (not Accepted without Comfy) |