# Co-Director M2.5 — Vision Validation Preflight

**Branch:** `phase2/codirector-m2-5-vision-validation`
**Base tip:** `phase2/codirector-m2-4-intelligence`
**Date:** 2026-07-24

## Hygiene

| Check | Result |
|---|---|
| `capability_bridge.py` encoding | Valid UTF-8 (compiles; no NUL bytes) |
| `storyboard.py` encoding | Valid UTF-8 |
| Working tree at branch cut | Carried forward M2.4.1 capability-bridge unification WIP |
| Migration slot | **M006** reserved for vision tables; **M010** untouched |

## Locked decisions (confirmed)

- Feature flag: `vision_validation_v1` → env `STUDIO_FEATURE_VISION_VALIDATION_V1` (default **OFF**)
- Migration revision: **M006** (`m006_codirector_vision.py`)
- No auto-regenerate, no auto-approve, no silent Bible mutation
- Corrections via existing M2.2 `tool_call` `ProposalService`
- Providers: `mock` (deterministic) + `local` (OpenCV/Pillow technical; ML stubs → `inconclusive`)
- APIs: `/api/codirector/vision/*` mounted via `routers/codirector.py`
- Never overload `CoDirectorSpecialistFinding.validation_status` for asset visual review
- M2.5 is the first code allowed to clear `visualValidationPending` on production plans

## Baseline hooks already present

- `ProductionPlan.visualValidationPending` (M2.4 intelligence schemas + store)
- Storyboard apply returns `visualValidationPending: true` without creating assets
- Health operator note placeholder for visual validation (to be wired to flag in M2.5)

## Out of scope for this milestone

- Cloud vision providers (OpenAI / Anthropic / Gemini)
- Automatic regeneration loops
- Real-person biometric identity
- Claiming visual certainty when models are absent
