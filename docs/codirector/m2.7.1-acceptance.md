# M2.7.1 Acceptance Checklist

M2.7 is **accepted only when M2.7.1** criteria below pass on **real providers** (live ComfyUI + real vision). Mock ImageGen completion is **disabled** on the Adept UI runtime path.

**Tip SHA (code under test):** `39c2abfd5b82b5444b526a4d964235366c5c5499` (short: `39c2abf`)
**Status: Accepted** — Full real Dashboard-to-Canon Playwright passed (`--retries=0`); live closed-loop on operator API used Z-Image Turbo (`z_image_turbo_bf16.safetensors`, UNET path); no `ADEPT_MOCK_IMAGEGEN`; no storyboard-capability skips.

**Acceptance run:** 2026-07-26 (local operator stack + Playwright e2e-start)

---

**Suites**

- Pytest: `studio-api/tests/test_production_executive.py` (+ migrations / vision / timeline refs / closed_loop_m2_6_1)
  - Schema / migration / queue / pause-cancel unit tests run without Comfy
  - Closed-loop / image integration tests **skip** unless ADEPT_REQUIRE_REAL_IMAGEGEN=1 and live Comfy /system_stats is reachable — they must **not** pass via mocks
- Playwright: `tests/e2e/codirector/production-executive-m27.spec.ts`
  - Probes `comfyui.health` AND `storyboard.generate` (and `codirector.vision.validate` when exposed); skips with explicit infrastructure reason when unavailable
  - Uses `provider: "local"` (not mock); no `ADEPT_MOCK_IMAGEGEN=1` default in `e2e-start`

---

## Live closed-loop evidence (operator API :8000)

| Field | Value |
|-------|--------|
| projectId | `d78c86fd-cf18-4c85-b5e3-422d4b6784bf` |
| sceneId | `667fce2f-ed10-4acd-bba3-e0a208577e67` |
| productionContextId | `88941261-b587-4391-ad4b-310b481eab28` |
| studio imagegen job | `5811d348-c8d4-49b2-9904-e590e5d0d4b1` |
| Comfy prompt_id | `2efd20c8-fc58-4db8-9b52-47866494e470` |
| model / UNET | `zimage` / `z_image_turbo_bf16.safetensors` |
| assetId | `5edf177e-d06e-4dc5-8ab5-5f7c940fb764` |
| mockAdapter | `false` |
| vision sessionId / reportId | `df60ada1-5ac9-4173-8d53-b642fc81b76a` / `fbd84111-aa74-4af6-b1ed-76011e54fb8f` |
| proposalId | `b47bfed7-a7af-452e-9e3a-c909e78cd5dd` |
| await_approval | `NeedsReview` until `mark-approval` (`autoApproved: false`) |
| apply_canon receiptId | `908d2f48-8661-4bcc-97b9-5dd3c5bd1b12` |
| bibleId / versionId | `f3c49c21-e259-4a09-ad8a-71aa07968ef7` / `347de228-7a19-4dba-a042-20a3df68d425` |
| ADEPT_MOCK_IMAGEGEN | unset |

Chain: storyboard_generate → image_generate (Asset) → validate (M2.5 local) → create_proposal → await_approval (NeedsReview) → explicit mark-approval → apply_canon via `ProposalService.approve`. Events, notifications, attempts, and audit history observed.

---

## Playwright (`--retries=0`)

| Result | Count |
|--------|------:|
| passed | 9 |
| failed | 0 |
| skipped | 2 |

Skip reasons (not capability/infra):
1. `flag off shows Off and hides dashboard` — flag is on in e2e-start (`production executive flag is on — flag-off UI covered when off`)
2. `restart recovery documented in pytest` — intentional (`Restart recovery covered by pytest crash recovery test`)

**Dashboard to Canon: real asset validation proposal receipt** — **passed** (no storyboard-capability skip).

---

## Pytest regression

| Suite | Result |
|-------|--------|
| `test_production_executive.py` | 20 passed, 5 skipped (gated: `ADEPT_REQUIRE_REAL_IMAGEGEN` not set) |
| `test_migrations.py` | 3 passed |
| `test_codirector_vision.py` | 10 passed |
| `test_timeline_reference_bindings.py` | 8 passed |
| `test_closed_loop_m2_6_1.py` | 17 passed |
| **Combined** | **58 passed, 5 skipped, 0 failed** |

## studio-web

- `npx tsc --noEmit` — pass
- `npm run build` — pass

---

## Checklist

| Scenario | Expectation | Pytest | Playwright |
|----------|-------------|--------|------------|
| **Flag off** | APIs 404; UI shows Off / dashboard hidden | `test_feature_flag_defaults_off`, `test_api_flag_off_404` | `flag off shows Off and hides dashboard` (skipped when flag on) |
| **Production Context** | Created once; same id on all jobs; sparse omit; immutable + extensions | `test_closed_loop_creates_production_context`, `test_production_context_immutable_and_sparse`, `test_m011_migration_creates_context_tables` | closed-loop response asserts `productionContext` |
| **Closed-loop (real)** | Real Comfy Job+Asset + vision + ProposalService; no mockAdapter | gated unit + **live API evidence** | `closed loop drains to NeedsReview with real artifacts` |
| **NeedsReview** | `await_approval` stops until explicit signal | gated await tests | same |
| **Canon unchanged until approve** | No Bible mutation before signal | gated | `canon unchanged until mark-approval` |
| **Approve** | `mark-approval` external; `autoApproved: false` | gated API test | `mark-approval returns autoApproved false` |
| **Bible receipt** | After approve + drain, receipt present | gated + live | `Dashboard to Canon: real asset validation proposal receipt` |
| **Events / audit** | Persisted events + history | unit + gated | `events and audit history endpoints return data` |
| **Failure / retry / pause / cancel** | Honest transitions | unit tests | generic job scenarios (no Comfy required) |
| **Restart recovery** | Running recovered without duplicate attempts | `test_crash_restart_recovery_no_duplicate_attempts` | documented skip → pytest |
| **Idempotency** | Duplicate closed-loop create reuses | unit create + Playwright idempotency | `closed-loop idempotency reuses jobs` |

---

## Pass criteria

- [x] Flag default OFF proven
- [x] Production Context M011 registered; immutable contract covered
- [x] Closed-loop uses **real** ImageGen + vision when stack available (no silent mock completion)
- [x] NeedsReview gate holds without silent approve
- [x] Canon only after explicit approval; receipt present
- [x] Events/audit observable
- [x] Failure/retry, pause/resume, cancel proven
- [x] Restart recovery proven in pytest
- [x] Idempotency proven
- [x] Playwright real-provider suite executed — Dashboard-to-Canon **passed** without mocks / capability skips

---

## Related

- `docs/codirector/M2.7.1_ACCEPTANCE_REPORT.md` — standalone acceptance summary (verdict, SHAs, counts)
- `docs/codirector/m2.7.1-production-context.md`
- `docs/codirector/m2.7.1-integration.md`
- `docs/codirector/m2.7.1-completion.md`
- `docs/codirector/m2.7.1-storyboard-generate-live-validation.md`
- `docs/codirector/M2.7.1_CLOSURE_STATUS_REPORT.md`
