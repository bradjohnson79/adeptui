# PR: M2.6.1 Closed-Loop Integration

**Branch:** `phase2/codirector-m2-6-1-closed-loop-integration`  
**Base tip ancestry:** M2.5 `d2a697d` ⊂ M2.6 `a99258d` ⊂ this branch  
**Push status:** **BLOCKED** — no `origin` remote configured on this machine.

## Summary

- Close M2.5↔M2.6 product gaps: Validation Workspace passes `referenceSet`; Director Refs counts prefetch; E2E enables Vision+Refs+Intel; tool registry expectations updated.
- Render Co-Director assistant replies as sanitized HTML (markdown→DOMPurify subset).
- Prove closed loop with flag-matrix pytest, flows A–I, Playwright scenarios 1–10.
- Stop IC-LoRA from falsely gating timeline reference tools.

## Test plan

- [x] pytest closed-loop + vision + refs + migrations + tools registry + capability bridge
- [x] `studio-web` `tsc -b && vite build`
- [x] Playwright `closed-loop-m2-6-1.spec.ts` (flags on)
- [x] Flag matrix combinations
- [ ] Push/open PR when remote + target branch exist

## Rollback

Reset/revert to `checkpoint/pre-m2-6-1-closed-loop-integration` (`a99258d`) or tag `checkpoint/codirector-closed-loop-v1` parent as needed. No migration renumbering.

## M2.7 readiness

Ready with limitations — see completion doc. No M2.7 orchestration code in this PR.