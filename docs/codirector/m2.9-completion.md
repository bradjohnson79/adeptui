# M2.9 Completion Notes — Complete Native Production Suite

**Status:** Implementation in progress / **partial** — **NOT ACCEPTED**.  
**Branch:** `phase2/codirector-m2-9-production-suite`  
**Prerequisites honored:** M2.8 CONDITIONALLY ACCEPTED (not weakened); M2.7.1 Accepted (not reopened).  
**Out of scope:** M2.10 discovery, GitHub/HF scans, M3.0. **Do not mark READY FOR M2.10.**

## What shipped

### Foundation (M2.9.0)
- Default-off feature flags: `image_production_v1`, `frame_production_v1`, `video_production_v1`, `director_timeline_v1`, `lipsync_production_v1`, `audio_production_v1`, `editing_production_v1`, `render_production_v1`, `codirector_production_control_v1`
- Operator health exposure on `/api/health` (same pattern as M2.8)
- Capability registry IDs for image/frame/video/audio/mouth/lipsync/render (honest `partially_wired` / `backend_only`; **not** `production_ready`)
- Migration **M013** additive SQLite tables: `m29_asset_versions`, `m29_frame_records`, `m29_render_manifests`, `m29_audio_cues`, plus timeline proposals and control plans
- Executive `JobType` + handlers: `frame_generate`, `frame_sequence`, `video_generate`, `lipsync_generate`, `mouth_track_generate`, `audio_generate`, `audio_process`, `timeline_render`, `scene_render`, `edit_apply` (plus extended `image_generate` for `m29` payloads)
- Package `studio-api/app/codirector/m29/` with flag-gated router `/api/codirector/m29/...`
- Frontend Production Suite workspace + nav (flag-gated); user-facing "Director 2.0" comment renamed to "Director Timeline Generation"
- `ADEPT_M29_FIXTURE_MODE` supported in `e2e-start.mjs` without enabling M2.10 discovery; M2.9 flags default **off** in e2e-start unless overridden

### Slices (M2.9.1–M2.9.9) — connected fixture paths
| Slice | Path | Acceptance provider |
|-------|------|---------------------|
| Image | m29 image + executive | CI fixture when `ADEPT_M29_FIXTURE_MODE=1`; Comfy path still available for non-m29 closed-loop |
| Frames | frame records + jobs + UI | CI fixture |
| Video | m29 video jobs (t2v/i2v via `mode`) | CI fixture |
| Director Timeline | propose / approve / apply | Approval-aware; apply blocked until approve |
| Lip sync / mouth | generate + mouth track + rectangles | CI fixture |
| Audio | generate/process + cues | CI fixture |
| Edit / SFX / music | proposals + gated apply | Approval required for edit apply |
| Render | manifests + scene/timeline render jobs | CI fixture |
| Control | decompose multi-step → jobs | Deterministic fixture plan |

## Explicit non-claims
- Sections are **not** production-ready; fixture-only paths must not be accepted as local provider proof.
- M2.5 still owns `visualValidationPending` — m29 only tracks its own asset version statuses.
- Human approval boundaries remain: timeline apply, edit apply, image approve/publish.
- Full M2.9 Accepted checklist (real providers where available, section acceptance docs for every department) is **not** complete.

## Tests
- `studio-api/tests/test_m29_production_suite.py` — flags-off, M013, registry uniqueness, approval boundaries, fixture API jobs
- `tests/e2e/codirector/production-suite-m29.spec.ts` — flags-off hide, sections, image run, integrated journey (requires flags ON)

## Verdict
**M2.9 NOT ACCEPTED** — partial foundation + fixture-connected slices. Continue section hardening and real-provider wiring before any Accepted claim. **Not ready for M2.10.**