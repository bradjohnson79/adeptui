# Co-Director M2.8 — Completion Report

**Milestone:** M2.8 Capability Intelligence foundation  
**Branch:** `phase2/codirector-m2-8-capability-intelligence`  
**Verdict:** **CONDITIONALLY ACCEPTED**

## Summary

Fixture-capable vertical slice for Model Radar, compatibility, sandbox + promotion
(human-gated via M2.7 jobs), routing, Virtual Stage, Shot Profiles, Production Recipes,
and Location Spin metadata. Feature flags default **off**. Live HF/GitHub discovery
campaigns are **not** started (M2.10). M2.7.1 Production Executive remains Accepted and
unweakened. M2.5 continues to own `visualValidationPending`.

## Delivered

### Feature flags (default off)
- `STUDIO_FEATURE_MODEL_RADAR_V1`
- `STUDIO_FEATURE_SANDBOX_RUNTIME_V1`
- `STUDIO_FEATURE_VIRTUAL_STAGE_V1`
- `STUDIO_FEATURE_SHOT_PROFILES_V1`
- `STUDIO_FEATURE_PRODUCTION_RECIPE_V1`
- `STUDIO_FEATURE_LOCATION_SPIN_V1`

Exposed on `/api/health` operator object.

### Migration
- `M012` capability intelligence tables (`m28_*`)

### Package
- `studio-api/app/codirector/m28/` services + `/api/codirector/m28/...` router
- `ADEPT_M28_FIXTURE_MODE=1` deterministic fixtures (no remote code execution)

### M2.7 job types / handlers
- `model_discover`, `evaluate_compatibility`, `sandbox_plan`, `sandbox_install`,
  `sandbox_validate`, `sandbox_promote`, `recipe_stage`, `apply_shot_profile`
- Install/promote handlers require approval — no silent path

### Frontend
- `/model-radar`, `/virtual-stage` (nav gated by health flags)
- `ModelRadarWorkspace`, `VirtualStageWorkspace`, shot profile / sandbox / recipe panels

## Tests

### Pytest
- `test_m28_capability_intelligence.py` — pass
- `test_production_executive.py` — pass
- `test_codirector_vision.py` — pass
- `test_migrations.py` — pass (includes M012)

### Playwright (`capability-intelligence-m28.spec.ts`, `--retries=0`)

| Run | Flags | Result |
|-----|-------|--------|
| Default e2e-start (M2.8 flags ON) | on | **17 passed, 1 skipped** (Scenario 1) |
| Flags-off override | off | Scenario 1 **passed** |

Scenario 1 cannot share one server process with flags-on scenarios (flags are process-boot).
Flags-off API coverage also exists in unit test `test_flags_off_api_404`.

## Constraints honored
- No M2.10 discovery campaigns
- No M2.9 completion claims
- No M3.0
- No overwrite of `m2.10-preflight-checkpoint.md`
- M2.7 durable ops path used for sandbox install/promote and recipe stages
- CI uses fixtures/mocks

## Remaining gaps (conditional)
- Single Playwright invocation with default e2e-start skips Scenario 1; full 18 requires a
  second flags-off run (verified passing).
