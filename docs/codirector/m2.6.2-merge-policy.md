# M2.6.2 - Merge Policy & Branch Protection Audit

**Date:** 2026-07-25  
**Repo:** `bradjohnson79/adeptui`

## Session authorization

```text
Authorized: gh auth + release path (push, draft PR) + long-term branch structure
NOT authorized: merge
```

## Long-term branch model (established)

```text
main     -> stable releases
develop  -> active integration (GitHub default branch)
phase2/* -> milestone feature branches
```

| Branch | Purpose | Notes |
|--------|---------|-------|
| `main` | Stable releases | Created from `checkpoint/codirector-closed-loop-v1` (`62330a7`) |
| `develop` | Active integration | Tracks M2.6.1 docs-ahead tip; default branch |
| `phase2/*` | Milestone work | Kept; not deleted; PR #1 head remains on M2.6.1 |

Do not leave the GitHub default on a milestone branch such as `phase2/codirector-m2-5-vision-validation`.

## Local policy (always)

- No force-push to milestone branches or checkpoint tags
- No squash/rebase rewrite of verified M2.5/M2.6/M2.6.1 history for release cleanup
- Prefer draft PR first
- Do not delete checkpoint tags
- WIP branches excluded from release PR

## Remote protection

| Setting | Value |
|---------|-------|
| Default branch | `develop` |
| Stable release branch | `main` @ `62330a7` |
| Required reviews | none (branch not protected) |
| Required checks | GitGuardian Security Checks (pass); no `.github/` app workflows |
| Allow merge | Only after explicit user authorization |

## Merge readiness (current expectation)

```text
Conditionally ready for human review; merge blocked without explicit authorization
```

Draft PR #1 base is `develop`. Do not merge without explicit user authorization.