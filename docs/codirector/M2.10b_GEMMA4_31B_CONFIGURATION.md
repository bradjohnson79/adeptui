# M2.10b Gemma 4 31B Configuration

**Status:** PERSISTED — primary `gemma4:31b-it-qat` with fallback `gemma4:12b`.
**Branch:** `phase2/codirector-m2-9-production-suite`
**Start SHA:** `c805d0c5b0b9a535b386ea38d09198460996d828`
**Date:** 2026-07-26
**Provider Manifest sha256 (must remain unchanged):** `cf99d7e5a91d192891495dbbb6f24feead24e6a9aec3ad236258ab39a4bba7bc`
**Gate 6 product lock sha256:** `7f26170dafccae376ec6227211efd3450338b4c22cb95f5a4fc47c43d7a3419b` (install/execute/weight **false**)

## Product decision (addendum)

M2.10b is authorized to migrate Co-Director’s primary local reasoning engine from the previous 12B default to the installed Gemma 4 31B model, via a **configurable** provider architecture.

## Discovered / persisted model identity

| Field | Value |
| --- | --- |
| Tag | `gemma4:31b-it-qat` |
| Digest | `e0812a55773bfeac846b2d605b4d93638b8dfa7119d9587f3d91475afc78185e` |
| Size | 18851736234 bytes |
| Fallback | `gemma4:12b` |
| Ollama | `0.32.1` |
| Host GPU | NVIDIA GeForce RTX 5090 32GB |
| Config file | `data/codirector_config.json` |

## Persisted configuration (measured)

```json
{
  "endpoint": "http://127.0.0.1:11434",
  "selectedModel": "gemma4:31b-it-qat",
  "primaryModel": "gemma4:31b-it-qat",
  "fallbackModel": "gemma4:12b",
  "allowAutomaticModelDownload": false,
  "timeoutSec": 180.0
}
```

## Health / generate (Slice 6)

| Check | Result |
| --- | --- |
| Cold `/api/tags` (prior migration measurement) | ~**11.2 s**, HTTP **200** |
| Warm `/api/tags` (Slice 6 smoke) | **0.004 s**, HTTP **200** |
| Short `/api/chat` generate | **1.617 s**, model `gemma4:31b-it-qat`, response `M210B_OK`, fallback not used |
| Peak VRAM during generate | **31573 MiB** / 32607 MiB |

## Fallback policy

Fallback to `gemma4:12b` only when primary unavailable, fails health, fails load, times out, or user explicitly selects fallback. Every fallback must record requested model, actual model, reason, timestamp, session/request id, error, retry. Never label fallback output as Gemma 4 31B.

## Migration checklist

| Step | Status |
| --- | --- |
| Preserve prior config | DONE (12B remains installed as fallback) |
| Direct Ollama health test of `gemma4:31b-it-qat` | DONE (~11.2 s cold / 0.004 s warm, HTTP 200) |
| Persist primary/fallback | DONE (`codirector_config.json`) |
| Specialist agents use central provider | PARTIAL — config persisted; agents consume central store |
| Settings/diagnostics UX shows active model | PARTIAL / follow-on |
| Restart persistence | DONE — config file present after restart-safe checks |
| Rollback tested | DOCUMENTED — see rollback doc (procedure ready; emergency rollback not exercised in Slice 6) |

## Explicit non-actions

- No automatic download of alternate LLMs
- Do not delete `gemma4:12b`
- Do not modify Ollama model blobs
- Do not alter Provider Manifest
