# M2.10b Gemma 4 31B Rollback

**Status:** READY — procedure documented; Slice 6 did not exercise emergency rollback (primary healthy).
**Branch:** `phase2/codirector-m2-9-production-suite`
**Start SHA:** `c805d0c5b0b9a535b386ea38d09198460996d828`
**Date:** 2026-07-26
**Provider Manifest sha256 (must remain unchanged):** `cf99d7e5a91d192891495dbbb6f24feead24e6a9aec3ad236258ab39a4bba7bc`
**Gate 6 product lock sha256:** `7f26170dafccae376ec6227211efd3450338b4c22cb95f5a4fc47c43d7a3419b` (install/execute/weight **false**)
**Phase 0:** `READY FOR M2.10b SANDBOX` — `docs/codirector/m2.10b-phase0-preflight-checkpoint.md`

## Purpose

Restore Co-Director to the pre-migration primary brain (`gemma4:12b` / prior persisted settings) without code changes if possible, and without corrupting Production Bible, timeline, or media records.

## Pre-switch inventory (to capture before migration)

| Item | Value / location | Captured? |
| --- | --- | --- |
| Prior primary model | `gemma4:12b` (repo default `studio-api/app/config.py`) | PARTIAL |
| Prior `selectedModel` in `codirector_config.json` | was `gemma4:12b` class default; now primary `gemma4:31b-it-qat` | CAPTURED |
| Env `STUDIO_OLLAMA_MODEL` / equivalents | TBD | PENDING |
| Context / generation presets | TBD | PENDING |
| Agent routing overrides | TBD | PENDING |

## Rollback steps

1. Stop active Co-Director sessions that depend on the in-flight migration (if any).
2. Restore persistent provider config: `primaryModel` / `selectedModel` → `gemma4:12b` (or captured prior value).
3. Restore fallback behavior and context settings from the pre-switch inventory.
4. Clear any stale “active model” cache that falsely persists 31B as healthy when unavailable.
5. Restart API / studio process.
6. Health-check Ollama for `gemma4:12b`; confirm Co-Director diagnostics show actual model used.
7. Smoke: Production Bible retrieval + one structured plan schema validation.
8. Confirm media/timeline records unchanged (model-independent).

## Success criteria

| Criterion | Status |
| --- | --- |
| Previous primary restored | PENDING |
| Fallback behavior restored | PENDING |
| Context settings restored | PENDING |
| Agent routing restored | PENDING |
| Persistent config restored | PENDING |
| No Provider Manifest change | REQUIRED (`cf99d7e5a91d192891495dbbb6f24feead24e6a9aec3ad236258ab39a4bba7bc`) |
| Rollback completed without Ollama model deletion | REQUIRED |

## Explicit non-actions during rollback

- Do not delete `gemma4:31b-it-qat` or `gemma4:12b` from Ollama
- Do not download replacement models
- Do not mutate sandbox audio installs as part of brain rollback
