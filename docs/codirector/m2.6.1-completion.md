# M2.6.1 Closed-Loop Integration — Completion

**Date:** 2026-07-25  
**Branch:** `phase2/codirector-m2-6-1-closed-loop-integration`  
**Verdict:** Conditionally accepted (critical loop proven; GitHub push blocked by missing `origin`).

## Lineage

| Item | Value |
|------|-------|
| M2.5 tip | `d2a697d` |
| M2.6 tip | `a99258d` |
| Ancestry | `d2a697d` ancestor of `a99258d` (exit 0) |
| Pre-integration tag | `checkpoint/pre-m2-6-1-closed-loop-integration` → `a99258d` |
| Release tag | `checkpoint/codirector-closed-loop-v1` |
| Push | **blocked** — no `origin` |

## Delivered fixes

1. Assistant markdown → sanitized HTML (`assistantMarkdown.ts` + `CoDirectorMessage`).
2. Validation Workspace resolves package `referenceSet` into `visionValidate`.
3. DirectorTracks prefetches per-clip Refs counts.
4. `EXPECTED_*_TOOLS` includes M2.6 reference tools.
5. E2E harness enables Vision + Timeline References (+ Intel).
6. Capability bridge: references tools no longer require IC-LoRA ready.
7. Closed-loop pytest fixture/matrix/flows A–I + Playwright scenarios.

## Verification

| Suite | Result |
|-------|--------|
| Closed-loop pytest (17) | PASS |
| Vision + refs + migrations + tools registry + capability | PASS |
| studio-web build | PASS |
| Playwright closed-loop | PASS (1 flaky startup race) |

## Capability truth

- Reference persistence: yes (M007 COW)
- Provider-neutral packaging: yes
- Model-level role conditioning: degraded without IC-LoRA (honest)
- Local vision: mock pass; local inconclusive
- IC-LoRA: probed, never faked
- ComfyUI: environment-dependent; not required for closed-loop mock path

## M2.7 readiness: ready with limitations

M2.7 may orchestrate existing services:

- Timeline reference CRUD / packages
- Vision validate / approve / correction proposals
- M2.2 proposal bus
- Intelligence production plans + `visualValidationPending` (clear ownership remains M2.5)

Still sync / needs durable work later:

- Generation → validate chaining
- Correction execution / regen
- Multi-step approval orchestration
- Event bus beyond existing receipts

**Next migration ID (docs only): M008**

## Out of scope preserved

- No M2.7 code
- `AIVideoStudio-m241` untouched
- download-sources CLI sign-in parked on `wip/download-sources-cli-signin`