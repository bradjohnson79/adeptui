# M2.10.1 Architectural Fit Report

**Date:** 2026-07-26

Compared to frozen Native Capability Snapshot + Provider Manifest.

| Candidate | Capability | Snapshot comparison | Fit notes |
| --- | --- | --- | --- |
| `hexgrad/Kokoro-82M` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `ResembleAI/chatterbox` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `coqui/XTTS-v2` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `SWivid/F5-TTS` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `openbmb/VoxCPM2` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `onnx-community/Kokoro-82M-v1.0-ONNX` | `audio.dialogue.generate` | duplicates | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `RVC-Boss/GPT-SoVITS` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `myshell-ai/OpenVoice` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `2noise/ChatTTS` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `coqui-ai/TTS` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `OpenBMB/VoxCPM` | `audio.dialogue.generate` | duplicates | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `CorentinJ/Real-Time-Voice-Cloning` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `harry0703/MoneyPrinterTurbo` | `audio.dialogue.generate` | reject | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `unslothai/unsloth` | `audio.dialogue.generate` | reject | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `calesthio/OpenMontage` | `audio.dialogue.generate` | reject | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `babysor/MockingBird` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `k2-fsa/OmniVoice` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign` | `audio.dialogue.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `Stability-AI/stable-audio-tools` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `stabilityai/stable-audio-open-1.0` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `stabilityai/stable-audio-3-medium` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `haoheliu/AudioLDM2` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `cvssp/audioldm2` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `cvssp/audioldm-s-full-v2` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `declare-lab/tango` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `declare-lab/tango2` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `declare-lab/TangoFlux` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `open-mmlab/Amphion` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `hkchengrex/MMAudio` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `CocoBro/Foley-Omni` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `tencent/HunyuanVideo-Foley` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `lucidrains/audiolm-pytorch` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `haoheliu/AudioLDM` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `archinetai/audio-diffusion-pytorch` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `huggingface/diffusers` | `audio.sfx.generate` | reject | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `denizsafak/abogen` | `audio.sfx.generate` | reject | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `ACE-Step/Ace-Step1.5` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `Tencent-Hunyuan/HunyuanVideo-Foley` | `audio.sfx.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `facebook/musicgen-small` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `facebook/musicgen-medium` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `facebook/musicgen-large` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `facebook/musicgen-melody` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `ace-step/ACE-Step` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `ace-step/ACE-Step-1.5` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `ACE-Step/Ace-Step1.5` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `multimodal-art-projection/YuE` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `riffusion/riffusion-hobby` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `HeartMuLa/heartlib` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `stabilityai/stable-audio-3-small-music` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `cvssp/audioldm2-music` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `lucidrains/musiclm-pytorch` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `salu133445/musegan` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `ElectricAlexis/NotaGen` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `haoheliu/AudioLDM2` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `feizc/FluxMusic` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `stabilityai/stable-audio-open-1.0` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `Stability-AI/stable-audio-tools` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |
| `ACE-Step/acestep-5Hz-lm-4B` | `audio.music.generate` | fills_missing | Would require new provider adapter + M2.7 audio_generate wiring; must not silently override native IDs |

## Contract readiness

- Provider addon registry: MISSING (see m2.10-addon-integration-contracts.md) — needed before integrate.
- Job handler `audio_generate` exists but capability providerIds empty.
- M2.8 sandbox/promote machinery Available but **not authorized** this pass.

