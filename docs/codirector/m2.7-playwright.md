# M2.7 Playwright

Spec: `tests/e2e/codirector/production-executive-m27.spec.ts`

## Requirements

- API with `STUDIO_FEATURE_PRODUCTION_EXECUTIVE_V1=1`
- Mock providers (no real Comfy / LLM required for closed-loop API path)

## Command

```bash
npx playwright test tests/e2e/codirector/production-executive-m27.spec.ts
```

## Coverage

- Status strip `Exec On/Off`
- Flag-off hides dashboard
- Flag-on: closed-loop API → NeedsReview → mark-approval (`autoApproved: false`) → drain → dashboard History/Statistics visible