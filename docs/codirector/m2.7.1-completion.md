# M2.7.1 Completion Record

## Branch

`phase2/codirector-m2-7-production-executive`
Draft PR: https://github.com/bradjohnson79/adeptui/pull/2

**Tip SHA:** `0f18b3670b8b1e66bc7eb486411d6b7e8a05797e` (short: `0f18b36`)

**Status: Accepted** — Z-Image still provider + catalogue readiness + Playwright Dashboard-to-Canon green (`--retries=0`; no storyboard-capability skip).

## Acceptance gate

**M2.7 base shipped earlier, but M2.7 is accepted only when M2.7.1 passes on real providers** (see `m2.7.1-acceptance.md`). Mock ImageGen silent completion has been removed from the Adept UI runtime path.

**Status:** Implementation complete for demock + Production Context (M011) **and** Z-Image storyboard.generate readiness. **Accepted** on live Comfy + catalogued Z-Image + local vision + Playwright Dashboard-to-Canon.

---

## What shipped in M2.7.1 (closure)

- Typed handler contracts (`executive/contracts.py`) — defaults away from mock / fixtureProfile pass
- Real closed-loop handlers (in-process Session):
  - `storyboard_jobs.prepare_storyboard_generate`
  - ImageGen Job+Asset via `imagegen_adapter` (**no mock complete**; Comfy required)
  - `run_validation` with provider `local` (rejects `mock` on runtime path)
  - `ProposalService` create / get / approve
- Immutable **Production Context** (M011 / revision `M011`) created once per closed-loop; attached to every job
- Result chaining between dependent jobs
- Flag-aware worker lifespan; honest capability checks (**no** `comfyui.health` fake for ADEPT_MOCK_IMAGEGEN/STUDIO_E2E)
- `e2e-start.mjs` no longer defaults `ADEPT_MOCK_IMAGEGEN=1`
- UI closed-loop uses `provider: "local"`

---

## Philosophy gates

- Orchestration only - no rebuild of Bible / Proposal / Timeline / Refs / Vision / Bridge / Registry
- Never silent approve / silent canon / silent regenerate / silent mock ImageGen success
- `mark-approval` remains an external M2.2 signal (`autoApproved: false`)
- Feature flag `STUDIO_FEATURE_PRODUCTION_EXECUTIVE_V1` default **OFF**
- Capability blocks honest (`Blocked` + reason)

---

## Verification recorded

| Check | Result |
|-------|--------|
| Pytest `test_production_executive.py` | Run after demock — unit/schema expected green; closed-loop **skipped** without Comfy |
| Playwright `production-executive-m27.spec.ts` | Requires live Comfy; otherwise skip with infrastructure reason |
| studio-web tsc/build | Not re-run in this closure pass unless noted |

---

## Known gaps

- Full Accepted verdict blocked until real ComfyUI + vision stack executes closed-loop end-to-end
- Tip SHA / PR body updated on commit; Accepted blocked until `storyboard.generate` + ImageGen packs ready
- Probe requires `comfyui.health` AND `storyboard.generate` (vision when exposed)

## Related docs

- `docs/codirector/m2.7.1-production-context.md`
- `docs/codirector/m2.7.1-integration.md`
- `docs/codirector/m2.7.1-worker-lifecycle.md`
- `docs/codirector/m2.7.1-acceptance.md`
- `docs/codirector/M2.7.1_TASK_REPORT.md`