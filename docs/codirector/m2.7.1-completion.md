# M2.7.1 Completion Record

## Branch

`phase2/codirector-m2-7-production-executive`
Draft PR: https://github.com/bradjohnson79/adeptui/pull/2

## Acceptance gate

**M2.7 base shipped in an earlier commit series, but M2.7 is accepted only when M2.7.1 passes** (real wiring + acceptance checklist in `m2.7.1-acceptance.md`).

---

## What shipped in M2.7.1

- Typed handler contracts (`executive/contracts.py`)
- Real closed-loop handlers (in-process Session):
  - `storyboard_jobs.prepare_storyboard_generate`
  - ImageGen Job+Asset + mock adapter (`ADEPT_MOCK_IMAGEGEN` / `STUDIO_E2E`)
  - `run_validation` + `ReferencePackageBuilder` remap
  - `ProposalService` create / get / approve
- Result chaining between dependent jobs
- Flag-aware worker lifespan start/stop + `recover_running_jobs`
- Honest single-process worker (`max_concurrent=1`, no fake distributed leasing)
- Capability checks via real bridge; mock ImageGen bypasses `comfyui.health` when mock/e2e env set
- Expanded pytest coverage; Playwright scenarios aligned to acceptance list
- Docs: `m2.7.1-integration.md`, `m2.7.1-worker-lifecycle.md`, `m2.7.1-acceptance.md`, this file

---

## Philosophy gates

- Orchestration only - no rebuild of Bible / Proposal / Timeline / Refs / Vision / Bridge / Registry
- Never silent approve / silent canon / silent regenerate
- `mark-approval` remains an external M2.2 signal (`autoApproved: false`)
- `visualValidationPending` cleared only by M2.5 VisionEngine when `planId` is set
- Feature flag `STUDIO_FEATURE_PRODUCTION_EXECUTIVE_V1` default **OFF**
- Capability blocks honest (`Blocked` + reason)

---

## Verification recorded

| Check | Result |
|-------|--------|
| Pytest regression files | **59/59 passed** (production_executive 21 + migrations/vision/timeline/closed_loop) |
| Playwright production-executive-m27.spec.ts | **10 passed, 2 skipped** (flag-off when e2e enables flag; restart recovery in pytest) |
| studio-web tsc/build | **pass** |

Tip SHA is not inlined here - use git rev-parse HEAD / PR body after the docs commit.

## Known gaps

- Tip SHA omitted from completion record (avoid amend / 7th micro-commit); report at PR tip
- M2.8 domains explicitly out of scope


## Related docs

- `docs/codirector/m2.7.1-integration.md`
- `docs/codirector/m2.7.1-worker-lifecycle.md`
- `docs/codirector/m2.7.1-acceptance.md`
- `docs/codirector/m2.7-completion.md`
- `docs/codirector/M2.7_TASK_REPORT.md`
- `docs/codirector/M2.7.1_TASK_REPORT.md`
