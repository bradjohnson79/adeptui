# M2.8 Acceptance Report

**Milestone:** M2.8 Capability Intelligence foundation  
**Branch:** `phase2/codirector-m2-8-capability-intelligence`  
**Status:** **CONDITIONALLY ACCEPTED**  
**Acceptance date:** 2026-07-26 (local)

This report is the standalone acceptance summary for the completed M2.8 foundation run.
Detailed checklists live in the linked docs; this file records verdict, SHAs, suite
counts, residual gaps, and rollback posture. It does **not** authorize M2.9 completion
or M2.10 discovery.

---

## 1. Executive summary

M2.8 delivers a fixture-capable vertical slice for Model Radar, compatibility evaluation,
sandbox + human-gated promotion (via M2.7 jobs), routing, Virtual Stage, Shot Profiles,
Production Recipes, and Location Spin metadata. Feature flags default **off**. Live
Hugging Face / GitHub discovery campaigns are **not** started (M2.10). M2.7.1 Production
Executive remains Accepted and unweakened. M2.5 continues to own `visualValidationPending`.

Playwright default e2e-start: **17 passed / 1 skipped / 0 failed**. Separate flags-off
run: Scenario 1 **passed**. Pytest (m28 + executive + vision + migrations) and
studio-web `tsc` + build are green.

**Verdict: CONDITIONALLY ACCEPTED** — product review still required for full Accepted;
Scenario 1 requires a dual-boot flags-off process (verified).

---

## 2. Branch / SHA

| Item | Value |
| --- | --- |
| Branch | `phase2/codirector-m2-8-capability-intelligence` |
| Implementation tip (pre-docs) | `427ee90081dc4ed54e4f95300000db20dec4c268` (`427ee90`) |
| Docs tip | After this acceptance-docs commit: `git rev-parse HEAD` |
| Base intent | develop lineage via prior readiness / remediation commits |
| Merge | **Not merged** (draft PR only) |

---

## 3. Files changed (high level)

| Area | Paths |
| --- | --- |
| Migration | `studio-api/app/migrations/m012_capability_intelligence.py` |
| M28 package | `studio-api/app/codirector/m28/` (radar, compat, sandbox, promote, routing, recipes, shot profiles, virtual stage, location spin, API, fixtures, db) |
| Executive handlers | `studio-api/app/codirector/executive/handlers.py` (M2.8 job types) |
| Feature flags | `studio-api/app/feature_flags.py` (+ health operator exposure) |
| Frontend | `ModelRadarWorkspace`, `VirtualStageWorkspace`, routes `/model-radar` + `/virtual-stage`, chrome nav, `api.ts` clients |
| Pytest | `studio-api/tests/test_m28_capability_intelligence.py` (+ migrations coverage) |
| Playwright | `tests/e2e/codirector/capability-intelligence-m28.spec.ts` |
| Docs | `m2.8-completion.md`, `m2.8-playwright-acceptance.md`, this report, remediation update |

Untracked local helper scripts under `scripts/_*.py` are **not** part of the acceptance package.

---

## 4. Migrations

| Item | Detail |
| --- | --- |
| Revision | **M012** |
| Path | `studio-api/app/migrations/m012_capability_intelligence.py` |
| Scope | Additive `m28_*` tables: model entries, watchlist, compat evals, sandbox instances/plans/validations, promotion manifests, shot profiles + versions, recipes + stages, virtual stages, location spins |
| Rollback notes | Additive; drop `m28_*` tables to reverse (`reversible=False` in registry) |
| Tests | Covered by `tests/test_migrations.py` |

---

## 5. Capability contracts

| Capability | Contract surface |
| --- | --- |
| Model Radar | Discover / classify / watchlist via `/api/codirector/m28/...` + fixture mode |
| Compatibility | Evaluate compatibility verdicts and reasons |
| Sandbox runtime | Plan / install / validate lifecycle isolated from production Comfy |
| Promotion | Manifest + human gate before production promote |
| Routing | Co-Director model routing recommendations |
| Virtual Stage | Structured camera workflow |
| Shot Profiles | Create / version / associate / export / import / apply |
| Production Recipes | Multi-stage recipes via M2.7 jobs; resume without duplicating success |
| Location Spin | Metadata / coverage plan (no silent production mutation) |
| Fixture mode | `ADEPT_M28_FIXTURE_MODE=1` — deterministic, no remote code execution |

M2.5 continues to own visual validation lifecycle (`visualValidationPending`).

---

## 6. Job handlers

M2.8 job types registered on the M2.7 Production Executive path:

| Job type | Role |
| --- | --- |
| `model_discover` | Fixture / radar discovery into registry |
| `evaluate_compatibility` | Compatibility evaluation |
| `sandbox_plan` | Sandbox install plan |
| `sandbox_install` | Install — **requires approval** (no silent path) |
| `sandbox_validate` | Sandbox validation |
| `sandbox_promote` | Promote — **requires approval** (no silent path) |
| `recipe_stage` | Production recipe stage execution |
| `apply_shot_profile` | Apply shot profile via durable job |

Handlers live in `studio-api/app/codirector/executive/handlers.py` and call into
`studio-api/app/codirector/m28/` services.

---

## 7. Feature flags

All default **off**. Exposed on `/api/health` operator object.

| Flag | Domain |
| --- | --- |
| `STUDIO_FEATURE_MODEL_RADAR_V1` | Model Radar |
| `STUDIO_FEATURE_SANDBOX_RUNTIME_V1` | Sandbox runtime |
| `STUDIO_FEATURE_VIRTUAL_STAGE_V1` | Virtual Stage |
| `STUDIO_FEATURE_SHOT_PROFILES_V1` | Shot Profiles |
| `STUDIO_FEATURE_PRODUCTION_RECIPE_V1` | Production Recipes |
| `STUDIO_FEATURE_LOCATION_SPIN_V1` | Location Spin |

When off: M2.8 routes unavailable; discovery API returns 404; no background discovery.

---

## 8. UI sections

| Route / surface | Component | Gate |
| --- | --- | --- |
| `/model-radar` | `ModelRadarWorkspace` | Health flags / nav gated |
| `/virtual-stage` | `VirtualStageWorkspace` | Health flags / nav gated |
| Chrome nav | `StudioChrome` links | Flag-aware |
| Panels | Shot profile / sandbox / recipe surfaces via API clients in `api.ts` | Flag-aware |

---

## 9. Security / approval

- Sandbox install and promote require explicit approval — no silent path.
- Bible / canon apply remains gated through existing Proposal / approval flow (Scenario 16).
- No silent promote; sandbox path isolation asserted (Scenario 18).
- Fixture mode mocks HF/GitHub; no live downloads or remote code execution in CI/e2e.
- M2.7.1 acceptance boundaries not weakened.
- Original `m2.10-preflight-checkpoint.md` left immutable (untouched this pass).

---

## 10. Playwright scenarios

Suite: `tests/e2e/codirector/capability-intelligence-m28.spec.ts`  
Retries: `0`  
Fixture mode: `ADEPT_M28_FIXTURE_MODE=1`

| # | Scenario |
| --- | --- |
| 1 | Flags off: routes unavailable, no console errors, no background discovery |
| 2 | Model discovery to registry with classifications |
| 3 | Watchlist and compatibility evaluation |
| 4 | Sandbox plan approval boundary |
| 5 | Isolated sandbox lifecycle without touching production Comfy |
| 6 | Promotion remains human gated |
| 7 | Co-Director model routing recommendations |
| 8 | Production recipe multi-stage via M2.7 jobs |
| 9 | Virtual Stage structured camera workflow |
| 10 | Guided and professional edit same shot profile |
| 11 | Cinematic depth accept selected only |
| 12 | Lighting / atmosphere / grade in one profile |
| 13 | Shot profile associate / export / import |
| 14 | Apply shot profile via M2.7 job |
| 15 | M2.5 owns `visualValidationPending` lifecycle |
| 16 | Approval and Bible canon remain gated |
| 17 | Resume recipe without duplicating successful stages |
| 18 | Security boundaries: no silent promote, sandbox path, location spin |

Scenario 1 cannot share one server process with flags-on scenarios (flags are process-boot).
See [m2.8-playwright-acceptance.md](./m2.8-playwright-acceptance.md).

---

## 11. Exact pass / fail / skip

### Run A — default e2e-start (M2.8 flags ON)

| Metric | Count |
| --- | ---: |
| Scenarios executed | 18 |
| Passed | **17** |
| Failed | **0** |
| Skipped | **1** (Scenario 1 — flags off) |

### Run B — flags OFF override

Filter: `-g "1 flags off"` with M2.8 flags `=0`.

| Metric | Count |
| --- | ---: |
| Passed | **1** (Scenario 1) |
| Failed | **0** |
| Skipped | **0** |

### Combined

All **18** scenarios exercised across Runs A+B. Default single invocation remains
**17 pass / 1 skip / 0 fail**.

### Other suites (this acceptance package)

| Suite | Result |
| --- | --- |
| Pytest `test_m28_capability_intelligence.py` | pass |
| Pytest `test_production_executive.py` | pass |
| Pytest `test_codirector_vision.py` | pass |
| Pytest `test_migrations.py` (incl. M012) | pass |
| studio-web `tsc` + build | pass |

---

## 12. Known limitations

- Single Playwright invocation with default e2e-start skips Scenario 1; full 18 requires a second flags-off run (verified passing).
- Live HF/GitHub discovery campaigns are out of scope (M2.10) — fixtures/mocks only.
- M2.9 Complete Production Suite is **not** started or claimed Accepted.
- READY FOR M2.10 remains blocked until M2.8 **full Accepted** + M2.9 Accepted (and remaining remediation blockers).
- Product-review gate remains for promoting CONDITIONALLY ACCEPTED to full Accepted.

---

## 13. Rollback

| Lever | Action |
| --- | --- |
| Feature flags | Leave all six M2.8 flags off (default) — APIs 404 / UI unavailable |
| Migration | Additive M012; reverse by dropping `m28_*` tables if required (see migration rollback notes) |
| Code | Revert branch / do not merge draft PR |
| Checkpoints | Original [m2.10-preflight-checkpoint.md](./m2.10-preflight-checkpoint.md) remains immutable and untouched |
| M2.7.1 | No reopen / weaken — Production Executive Accepted path preserved |

---

## 14. Verdict

**CONDITIONALLY ACCEPTED**

Core connected E2E flow passes under fixture mode; Scenario 1 dual-boot verified;
pytest + tsc/build green. Full Accepted requires product review after conditional
acceptance. Does **not** claim READY FOR M2.10. Does **not** start M2.9 or M2.10
discovery. Does **not** weaken M2.7.1.

---

## Related docs

- [m2.8-completion.md](./m2.8-completion.md)
- [m2.8-playwright-acceptance.md](./m2.8-playwright-acceptance.md)
- [m2.8-prompt.md](./m2.8-prompt.md)
- [m2.9-readiness-remediation.md](./m2.9-readiness-remediation.md)
- [m2.10-preflight-checkpoint.md](./m2.10-preflight-checkpoint.md) (immutable; untouched)
- [M2.7.1_ACCEPTANCE_REPORT.md](./M2.7.1_ACCEPTANCE_REPORT.md)
