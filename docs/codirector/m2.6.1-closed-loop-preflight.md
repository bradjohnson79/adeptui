# M2.6.1 Closed-Loop Integration — Preflight

**Date:** 2026-07-25  
**Integration branch:** `phase2/codirector-m2-6-1-closed-loop-integration`  
**Base tip:** `a99258d` (M2.6 reference binding handoff)

## Commands captured

```text
git status                          → clean after parking download-sources WIP
git branch --show-current           → phase2/codirector-m2-6-1-closed-loop-integration
git log --oneline --decorate -30    → M2.6 tip a99258d; M2.5 tip d2a697d in ancestry
git worktree list                   → see inventory below
git tag --list "checkpoint/*"       → includes checkpoint/pre-m2-6-1-closed-loop-integration
git remote -v                       → (empty — no origin configured)
```

## Branch tips

| Branch | Tip | Notes |
|--------|-----|-------|
| `phase2/codirector-m2-5-vision-validation` | `d2a697d` | M2.5 completion |
| `phase2/codirector-m2-6-reference-binding` | `a99258d` | M2.6 completion |
| `phase2/codirector-m2-6-1-closed-loop-integration` | starts at `a99258d` | This milestone |
| `wip/download-sources-cli-signin` | `1f73d32` | Parked unrelated WIP (not on M2.6.1) |

## Ancestry

```powershell
git merge-base --is-ancestor d2a697d a99258d
# exit code 0 — M2.5 already contained in M2.6; no re-merge required
```

Accidental commit + revert history on the M2.6 line (`a7377c9` then `8c56f38`) is preserved.

## Worktree inventory

| Path | HEAD | Branch | Action |
|------|------|--------|--------|
| `C:/AdeptFilmWorks/AIVideoStudio` | `a99258d`+ | `phase2/codirector-m2-6-1-closed-loop-integration` | **Active** |
| `C:/AdeptFilmWorks/AIVideoStudio-m241` | `68387a5` | `phase2/codirector-m2-4-intelligence` | **Do not touch** |
| `C:/AdeptFilmWorks/AIVideoStudio-pack-authoring` | `9af9196` | `phase1c/essential-pack-authoring` | Untouched |
| `C:/AdeptFilmWorks/AIVideoStudio-psr` | `24384a4` | `phase2/production-systems-readiness` | Untouched |

## Checkpoint tags

| Tag | Target |
|-----|--------|
| `checkpoint/pre-m2-6-reference-binding` | `d2a697d` |
| `checkpoint/pre-m2-6-1-closed-loop-integration` | `a99258d` (annotated, created for this milestone) |

Final release tag `checkpoint/codirector-closed-loop-v1` is created only after critical green.

## Migration registry (at base)

```text
M001, M002, M003, M004, M005, M006, M007, M010
```

- **No M008 / M009 migration files** on this line (reserved in schema plan only).
- Ordering is **lexical** by revision string (`M007` < `M010`).
- See `docs/codirector/m2.6.1-migration-audit.md` for clean-install / upgrade results.

## Feature flags (defaults)

| Flag | Env | Default |
|------|-----|---------|
| Vision validation | `STUDIO_FEATURE_VISION_VALIDATION_V1` | off |
| Timeline references | `STUDIO_FEATURE_TIMELINE_REFERENCES_V1` | off |
| Intelligence v2 | `STUDIO_FEATURE_CODIRECTOR_INTELLIGENCE_V2` | off |

E2E harness previously enabled Intelligence only; M2.6.1 enables Vision + Timeline References in `scripts/e2e-start.mjs` (overridable via env) so closed-loop Playwright can run.

## Baseline test state (pre-integration)

- Pytest: M2.5 (`test_codirector_vision.py`) and M2.6 (`test_timeline_reference_bindings.py`) exist as vertical slices.
- Playwright: `timeline-references.spec.ts` (A–H) and `vision-storyboard-validation.spec.ts` exist separately.
- Gaps: no closed-loop fixture/spec; Validation Workspace omitted `referenceSet`; per-clip Refs counts only for selected clip; `EXPECTED_*_TOOLS` stale vs M2.6 registry; e2e flags incomplete.

## Known integration risks

1. UI validate path must pass M2.6 package `referenceSet` into M2.5 vision.
2. `visualValidationPending` clear ownership must remain M2.5-only.
3. Flag matrix must not produce blank apps / false readiness.
4. IC-LoRA must never be claimed ready without probe.
5. Download-sources CLI sign-in WIP must stay off this branch (parked on `wip/download-sources-cli-signin`).

## GitHub remote state

- **`git remote -v` is empty** — no `origin`.
- Push of `m2-5` / `m2-6` / `m2-6-1` branches and tags is **blocked** until a remote is configured.
- PR description is still authored at `docs/codirector/m2.6.1-pull-request-description.md` for later open against a target branch when remote exists.
- No local `develop` / `main` branch present for PR base.

## WIP hygiene

Unrelated dirty download-sources files were committed on `wip/download-sources-cli-signin` (`1f73d32`) before creating this branch from clean `a99258d`.
