# M2.6.1 Closed-Loop Smoke Test Results

**Date:** 2026-07-25

## Flags ON (E2E harness defaults after M2.6.1)

Env:

```text
STUDIO_FEATURE_VISION_VALIDATION_V1=1
STUDIO_FEATURE_TIMELINE_REFERENCES_V1=1
STUDIO_FEATURE_CODIRECTOR_INTELLIGENCE_V2=1
```

Results:

| Step | Result |
|------|--------|
| Health reports vision+refs+intel on | PASS (e2e-start) |
| Empty package for @Image1 | PASS |
| Multi-ref package for @Image2 | PASS |
| COW version increment | PASS |
| Vision validate with referenceSet annotations | PASS |
| Proposal before mutate | PASS |
| Approve applies binding | PASS |
| Vision approve + bibleLinkProposal | PASS |
| Package validate never blocksRun | PASS |

## Flags OFF

Product defaults remain off (`FeatureFlags.from_env({})`).

| Step | Result |
|------|--------|
| Defaults off | PASS (pytest) |
| Flag matrix health honesty | PASS (5 combos) |
| Refs/vision routes gated | PASS (existing M2.5/M2.6 tests) |

## Notes

- Scenario 1 can flake once if Playwright starts before API bind; retry passes.
- Use matching `STUDIO_API_BASE` / `STUDIO_API_PORT` when overriding ports.