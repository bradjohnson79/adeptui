# M2.6 — Smoke Test

## Preconditions

- Branch `phase2/codirector-m2-6-reference-binding`
- `STUDIO_FEATURE_TIMELINE_REFERENCES_V1=1`
- API + web running

## Procedure

1. Open a project scene Director Prompt Timeline.
2. Add two images → confirm `@Image1` / `@Image2` on clip chrome.
3. Select `@Image1` → References panel shows `Refs: 0`.
4. Add a style reference from Project Assets → count becomes 1; COW version 1.
5. Change influence → version increments; prior version restorable via API.
6. GET reference-package → `primaryFrame` present; `referenceSet` non-null; Run not blocked.
7. Flag off → panel hidden; references routes 404.
8. Co-Director (flag on): `list_timeline_images` returns tags; mutating tools create proposals only.

## Results

Recorded during automated pytest `test_timeline_reference_bindings.py` (8 passed) and Playwright `timeline-references.spec.ts` (A–G; H when vision flag on).
