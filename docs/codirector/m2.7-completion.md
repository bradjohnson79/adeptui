# M2.7 Completion

## Acceptance status

**M2.7 base shipped**, but **M2.7 is accepted only when M2.7.1 passes**.

See:

- `docs/codirector/m2.7.1-integration.md`
- `docs/codirector/m2.7.1-worker-lifecycle.md`
- `docs/codirector/m2.7.1-acceptance.md`
- `docs/codirector/m2.7.1-completion.md`

Handlers are now **real-wired** (not mocks): storyboard prepare, Job+Asset ImageGen (mock adapter under `ADEPT_MOCK_IMAGEGEN`/`STUDIO_E2E`), `run_validation` + refs remap, and `ProposalService` create/get/approve — all in-process Session, no self-HTTP.

---

## Delivered (M2.7 base)

- [x] Branch from develop tip (not milestone-only / not AIVideoStudio-m241)
- [x] Feature flag `STUDIO_FEATURE_PRODUCTION_EXECUTIVE_V1` default off
- [x] Schema **M008** (jobs, attempts, deps, events, notifications, audit)
- [x] Durable engine package under `studio-api/app/codirector/executive/`
- [x] APIs under `/api/codirector/jobs` (404 when flag off)
- [x] Co-Director Production Executive dashboard (7 tabs)
- [x] Pytest orchestration coverage (expanded in M2.7.1)
- [x] Playwright e2e spec for closed loop (scenarios expanded in M2.7.1)
- [x] Docs: architecture, job model, events, dashboard, api, smoke, playwright, completion

## Philosophy gates

- Orchestration only - no rebuild of Bible/Proposal/Timeline/Refs/Vision/Bridge/Registry
- Never auto-approve / silent canon / silent regenerate
- `mark-approval` is an external M2.2 signal; Executive returns `autoApproved: false`
- Capability blocks are honest (`Blocked` + reason)
- `visualValidationPending` cleared only by M2.5 VisionEngine when `planId` is set

## M2.7.1 closes the wiring gap

- Closed-loop handlers call real services (see m2.7.1-integration)
- Worker lifecycle in `main.py` lifespan (see m2.7.1-worker-lifecycle)
- Acceptance checklist maps pytest + Playwright (see m2.7.1-acceptance)

## Key paths

- `studio-api/app/migrations/m008_production_executive.py`
- `studio-api/app/codirector/executive/*` (including `contracts.py`, `imagegen_adapter.py`)
- `studio-api/app/storyboard_jobs.py`
- `studio-web/src/components/CoDirector/CoDirectorProductionExecutive.tsx`
- `studio-api/tests/test_production_executive.py`
- `tests/e2e/codirector/production-executive-m27.spec.ts`
