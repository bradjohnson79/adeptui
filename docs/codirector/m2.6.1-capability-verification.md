# M2.6.1 Capability Verification

## Honesty rules verified

| Claim | Evidence |
|-------|----------|
| Disabled flags ≠ operationally ready | `test_disabled_flags_are_not_operationally_ready` + flag matrix health |
| Timeline refs tools do not require fake IC-LoRA | capability bridge maps `references` → `references.timeline_bindings` only |
| IC-LoRA never claimed ready without probe | package `support.icLoraReady` / degraded unsupportedBindings |
| Local vision inconclusive ≠ pass | existing `test_local_provider_ml_stubs_inconclusive` |
| Vision validate / review capabilities mapped | `psr_ids_for_tool_key("vision")` |

## PSR IDs (tool keys)

| Tool key | PSR IDs |
|----------|---------|
| references | `references.timeline_bindings` |
| vision | `codirector.vision.validate`, `codirector.vision.review` |

## Degraded states

- Package support may be `degraded` when IC-LoRA unavailable; `blocksRun` remains false.
- Playwright Scenario 10 asserts this honesty.