# M2.7 Smoke Test

## Preflight

- Branch: `phase2/codirector-m2-7-production-executive` from `develop` tip `d241575`
- Enable: `STUDIO_FEATURE_PRODUCTION_EXECUTIVE_V1=1`

## Commands

```bash
cd studio-api
python -m pytest tests/test_production_executive.py tests/test_migrations.py -q
```

## Results (2026-07-25)

```
tests/test_production_executive.py ..............  14 passed
tests/test_migrations.py ...                      (includes M008 in registry)
```

## Manual API loop

```bash
# create closed loop
curl -X POST http://127.0.0.1:8000/api/codirector/jobs/closed-loop \
  -H "Content-Type: application/json" \
  -d "{\"projectId\":\"<id>\",\"sceneId\":\"scene-1\",\"provider\":\"mock\"}"

# drain
curl -X POST http://127.0.0.1:8000/api/codirector/jobs/worker/drain \
  -H "Content-Type: application/json" \
  -d "{\"maxSteps\":40}"

# mark approval on await_approval job (never auto)
curl -X POST http://127.0.0.1:8000/api/codirector/jobs/<awaitJobId>/mark-approval \
  -H "Content-Type: application/json" \
  -d "{\"proposalId\":\"prop-scene-1\",\"approved\":true}"
```

Expect `await_approval` → `NeedsReview` before mark-approval; `apply_canon.result.applied == false`.