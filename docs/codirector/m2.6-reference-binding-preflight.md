# Co-Director M2.6 — Timeline Reference Binding Preflight

**Date:** 2026-07-24  
**Branch:** phase2/codirector-m2-6-reference-binding  
**Checkpoint:** checkpoint/pre-m2-6-reference-binding @ d2a697d  
**Base tip:** phase2/codirector-m2-5-vision-validation (M2.5 complete)

## Baseline

| Item | State |
|---|---|
| HEAD | d2a697d — docs(codirector): complete M2.5 vision validation handoff |
| Working tree | Clean at branch cut |
| Migrations | M001–M006 + M010 present; **next is M007** (do not touch M006/M010) |
| Flags | codirector_intelligence_v2, ision_validation_v1 present; add 	imeline_references_v1 |
| Director model | ImageClip in scene.director_json; GET/PUT …/scenes/{id}/director |
| FE labels | DirectorTracks.freeImageClips still forces index-based Image N |
| Bible refs | Entity 
eference_link + generation package (project/scene scope, not per-item) |
| IC-LoRA | pp/references/ roles/strengths — keep; map timeline bindings when capable |
| M2.5 vision | Package under codirector/vision/; extend ValidateRequest with optional 
eferenceSet |
| Encoding | Spot-checked M2.5 vision + M006 as UTF-8 (BOM on some docs). Fix to UTF-8 if UTF-16 appears during work. |

## Locked decisions (confirmed)

1. Flag: 	imeline_references_v1 → STUDIO_FEATURE_TIMELINE_REFERENCES_V1 (default **OFF**)
2. Migration: **M007** m007_timeline_reference_bindings.py
3. SQL for Reference Sets (COW); timeline JSON gains display_tag + 
ext_image_tag_number
4. APIs under /api/projects/{projectId}/scenes/{sceneId}/director/items/{itemId}/…
5. UI mutations = direct REST; Co-Director mutations = M2.2 proposals only
6. Inheritance: item-specific only; document Project→Sequence→Scene extension points
7. Presets: lightweight asset-ID links
8. Sources: Bible, Project Assets, Timeline Images (cycle-safe), Approved frames, Upload
9. Never require refs to Run; never clear isualValidationPending outside M2.5; never fake IC-LoRA readiness

## Risks

| Risk | Mitigation |
|---|---|
| Tag renumbering on delete/move | Monotonic 
ext_image_tag_number; never reclaim |
| Timeline↔timeline reference cycles | Detect in service; 
eference_cycle_detected |
| Flag-off leakage | Gate all new REST + hide Reference Workspace UI |
| UTF-16 corruption in Cursor/tooling | Verify with Python 
ead_bytes; rewrite UTF-8 if needed |
| Confusing primary frame with supporting refs | Package keeps primaryFrame separate; empty set → 
eferenceSet: null |
| Fake generation readiness | Capability baselines honest; reuse 
eferences.ic_lora.ready probe |

## Architecture sketch

`	ext
ImageClip(+display_tag) --> primaryFrame
         |
         +--> ReferenceSet (COW versions) --> bindings (role + influence)
                          |
                          v
              ProviderNeutralReferencePackage
                     |            |
                     v            v
              Generation adapters   M2.5 ValidateRequest.referenceSet
`

## What to reuse

- Director timeline parse/dump + PUT path
- pp/references/ IC-LoRA (do not replace)
- M2.2 proposal bus for Co-Director mutating tools
- M2.5 vision engine/contract (optional 
eferenceSet)
- PSR capabilities/registry.py pattern

## What to invent (this milestone)

- Stable @ImageN tags
- SQL Reference Sets + presets (M007)
- Reference Workspace UI
- Provider-neutral package builder
- Co-Director tag resolve + tools
- Binding-aware vision validation contract

## Feature flag

	imeline_references_v1 → env STUDIO_FEATURE_TIMELINE_REFERENCES_V1 (default off).
