# M2.12 Adaptive Learning, Self-Correction, and Evolution — Task Report

**Date:** 2026-07-26
**Tip SHA:** 03d73e04e0860a61182535a3f415bf094aff052c
**Branch:** `phase2/codirector-m2-9-production-suite`
**Verdict:** ACCEPTED

## Executive summary

M2.12 adds an Adaptive Learning layer on top of M2.11 memory/decisions/traces and the existing `learning.py` project preferences. Evolution is **structured lessons + versioned strategy packs**, not Gemma weight updates. Feature flag `codirector_adaptive_learning_v1` defaults **OFF**. System-layer lessons never auto-activate.

## Feature flag

| Field | Value |
| --- | --- |
| Env | `STUDIO_FEATURE_CODIRECTOR_ADAPTIVE_LEARNING_V1` |
| Flag | `codirector_adaptive_learning_v1` |
| Default | **OFF** |
| Flag-off API | `/api/codirector/m212/*` → **404** |

## Lesson layers

`session` | `project` | `user` | `system`

## Architecture

Production action → outcome → critique → mistake class → candidate lesson → evidence score → regression gate → human/policy approval → active lesson injection → future decisions improve.

Hard bans (enforced in `m212/safety.py`): no fine-tuning, no autonomous source rewrite, no Manifest/execution-lock edits, no silent asset mutation, no system auto-activate, no silent lesson delete, no learning from unverified failed outputs without classification.

## Key artifacts

- `studio-api/app/codirector/m212/` — safety, lessons, critique, promote, regression, calibration, strategy, API
- `studio-api/app/migrations/m015_adaptive_learning.py` — M015 tables
- `studio-web/src/components/LearningEvolutionPanel.tsx` — Learning Evolution UI
- Bridge into `learning.py` LearnedItem for project-layer promotes
- Injection into `m211/context_pack.py` + chat `learning_context_block` / `adaptive_lessons_block` when flag ON

## Smoke

Reject → critique → candidate → regression → human project promote → retrieve in M2.11 orchestration → rollback; Manifest sha unchanged; learning cannot write Manifest/locks.

## Provider Manifest

| Field | Value |
| --- | --- |
| Path | `config/capabilities/adept-ui-v1.0-provider-manifest.json` |
| sha256 | `cf99d7e5a91d192891495dbbb6f24feead24e6a9aec3ad236258ab39a4bba7bc` |
| Changed | **NO** |

## Explicit non-actions

- No model fine-tuning / weight updates
- No autonomous production-code rewriting
- No provider promotion / execution-lock / Manifest edits
- No silent timeline / Bible / asset mutation

## Roadmap

M2.10b → M2.11 → **M2.12** → M3.0 (failure recognition + non-repetition)

## Known limitations

- Default-off; requires explicit env enablement
- Critique classification is heuristic (no LLM required for smoke)
- Session-layer auto-expire is soft (status/session scoped retrieval; no background janitor)
- User-layer cross-project identity is a simple `user_id` string (no SSO binding)
- Strategy packs influence context; they do not retrain specialists

## Tests

	est_m212_adaptive_learning.py — **P=13 / F=0 / S=0** (exit 0)

## Smoke

**PASS** (reject → critique → candidate → regression → promote → orchestrate retrieve → rollback; Manifest unchanged)
