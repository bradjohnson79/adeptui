# Testing

- Unit: matrix, eligibility, aggregation, thresholds, stale evidence, auto-mode safety, rollback
- Integration: suite → queue → persist → review → evaluate → promote/rollback → recommend
- Playwright: `tests/e2e/m42/m42-codirector-prompt-intelligence-v2.spec.ts`
- Live: ≥1 real image + video + audio/voice path (mocks cannot replace)
