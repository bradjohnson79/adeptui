# Co-Director Unified Experience Architecture

Flag: `codirector_unified_experience_v1` / `STUDIO_FEATURE_CODIRECTOR_UNIFIED_EXPERIENCE_V1` (default OFF).

Package: `studio-api/app/codirector/m214/`. Extends M2.11 DAG/selector; does not fork orchestration.

Layout: left stage nav, center structured conversation + media rail, right collapsible inspector (plan + Approval Center).
