# M2.6 — Playwright Coverage

Spec: `tests/e2e/director/timeline-references.spec.ts`

| Test | Coverage |
|---|---|
| A–G | Stable tags, flag gate, empty set, add binding, package shape, validate never blocks Run, COW patch; UI best-effort |
| H | Vision `referenceSet` contract when `visionValidationEnabled` |

Run (example):

```powershell
npx playwright test tests/e2e/director/timeline-references.spec.ts
```

Requires API with appropriate feature flags for full path.
