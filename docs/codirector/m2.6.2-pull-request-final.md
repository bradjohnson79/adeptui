# Pull Request — Co-Director M2.5–M2.6.1 Closed-Loop Integration

**Status:** Prepared for draft open after auth + push. Merge is **not** authorized.

## Final PR title

```text
Co-Director M2.5–M2.6.1: Closed-Loop Vision and Timeline Reference Integration
```

## Branches

| Role | Branch |
|------|--------|
| Source | `phase2/codirector-m2-6-1-closed-loop-integration` |
| Target | `<TARGET_BRANCH>` — prefer remote default if empty repo creates `main`/`master` via first push policy; otherwise evidence from `gh repo view` |

If the remote is empty, GitHub may require an initial default branch. Prefer opening the draft PR against the repository default branch identified by:

```powershell
gh repo view bradjohnson79/adeptui --json defaultBranchRef
```

## Summary

Integrates optional timeline Reference Sets, provider-neutral reference packages,
binding-aware visual validation, proposal-only correction workflows, human
production approval, and optional Production Bible canon linking.

Also includes M2.5↔M2.6 gap fixes, sanitized assistant HTML, closed-loop pytest/Playwright proof, IC-LoRA honesty for reference tools, and M2.6.1/M2.6.2 release docs.

## Lineage / tags

```text
d2a697d ⊂ a99258d ⊂ 62330a7 ⊂ source tip
checkpoint/codirector-closed-loop-v1 → 62330a7
```

Source tip may include docs-only commits after the closed-loop tag; tag is not moved.

## Test plan

- [x] Local closed-loop pytest / build / Playwright (M2.6.1)
- [ ] Remote branches + tags pushed (no force)
- [ ] Draft PR opened
- [ ] Remote CI recorded honestly

## Excluded WIP

```text
wip/download-sources-cli-signin
wip/home-page-images
```

## Rollback

- `checkpoint/pre-m2-6-1-closed-loop-integration` (`a99258d`)
- `checkpoint/codirector-closed-loop-v1` (`62330a7`)

## Draft create (after push)

```powershell
$gh = "C:\Program Files\GitHub CLI\gh.exe"
& $gh pr list --repo bradjohnson79/adeptui --head phase2/codirector-m2-6-1-closed-loop-integration
& $gh pr create --repo bradjohnson79/adeptui --draft `
  --base <TARGET_BRANCH> `
  --head phase2/codirector-m2-6-1-closed-loop-integration `
  --title "Co-Director M2.5-M2.6.1: Closed-Loop Vision and Timeline Reference Integration" `
  --body-file docs/codirector/m2.6.2-pull-request-final.md
```

Do **not** merge unless the user explicitly authorizes merge later.
