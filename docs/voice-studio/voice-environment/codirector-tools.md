# Co-Director Tools (frozen IDs)

See `VOICE_ENVIRONMENT_READ_TOOLS` and `VOICE_ENVIRONMENT_MUTATION_TOOLS` in `studio-web/src/contracts/voiceEnvironment.ts`.

Mutations are proposal-gated. Reads must use live persisted state.
