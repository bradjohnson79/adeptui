# Scene and Spatial Integration

`POST /api/voice-environment/recommend` reads scene context and best-effort Spatial Map environment prompts to propose space/device/walla. Creator must review and apply. No silent mutations.
