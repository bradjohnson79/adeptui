# Timeline and Lip Sync

Timeline handoff carries dry + processed + room tone + walla + `VoiceEnvironmentTiming`. Spoken start uses dry/`speechStartOffsetMs`. Lip Sync always uses dry audio timing; tails ignored.
