# M42 Wave 4 — Version History & Approval

Non-destructive version graph with states:

`	ext
Draft → PendingReview → Approved → ProductionMaster
         ↘ Rejected → (branch or Archive)
`

Review notes (uthor, 	ext, createdAt) attach before approval. Production Master promotion is explicit. Visual history thumbnails stored under project edit history / gate preview reuse.

Artifacts: ersion_history_results.json (pprovalPipelineOperational: true), isual_history_results.json.
