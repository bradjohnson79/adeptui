# M42 Wave 3 — Storyboard & Asset Library

## Storyboard

`prepare_storyboard_generate` / `enqueue_imagegen_job` compile through Image Product with purpose `storyboard`, preset `builtin-storyboard`, and scene/shot/camera/continuity IDs in intent metadata.

## Asset Library

- Project-scoped favorites  
- Filters: modelFamily, hasReferences, collectionId  
- Production Collections CRUD  
- ImageProvenance / generation-history inspector  
- Gate preview thumbnails  

Artifacts: `storyboard_results.json`, `asset_library_results.json`, `collections_results.json`.
