# M42 Wave 4B — Fullscreen & Docking

- Fullscreen button with browser Fullscreen API + fixed overlay fallback
- Escape exits; focus returns
- Left/right/timeline resize with a11y separators
- Pane move Left/Right + reorder Up/Down (menu-based; no floating windows)
- Dock collapse/restore; presets; Reset Workspace; localStorage persistence
