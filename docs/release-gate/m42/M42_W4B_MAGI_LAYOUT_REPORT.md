# M42 Wave 4B — MAGI Layout Report

Target hierarchy: Hero → Left dock (~18%) | Viewer (~62%) | Inspector (~20%) → full-width Timeline (~280px) → collapsed Render Queue.

Implemented: resizable splitters, dock collapse, pane move left/right, presets, persistence (`MagiWorkspaceLayoutV1`), taller Korri banner `clamp(300px, 28vw, 420px)` with focal point `center 38%`.
